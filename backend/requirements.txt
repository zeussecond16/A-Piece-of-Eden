# Dependensi Python untuk A Piece of Eden Backend
# Install dengan perintah: pip install -r requirements.txt

fastapi>=0.115.5
uvicorn>=0.30.0
python-multipart>=0.0.12
pydantic>=2.11.0
httpx
python-dotenv
supabase

# ── Untuk porting ke Supabase nanti ──────────
# Hapus tanda # di bawah saat sudah siap porting:
# supabase>=2.10.0
